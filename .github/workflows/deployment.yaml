name: Deployment
run-name: "[${{ inputs.env }}] - Terraform Deployment"

on:
  workflow_dispatch:
    inputs:
      env:
        type: choice
        options:
          - dev
          - int
          - prod
    
jobs:
  checkout:
    name: "Checkout Repositories"
    uses: mschmalzbauer/.github/.github/workflows/checkout.yaml@main
